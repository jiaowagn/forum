- @posts.each do |post|
  %h1= link_to post.title, post_path(post)
  %p
    Published at
    = time_ago_in_words(post.created_at)
    ago by
    = post.user.email

- if user_signed_in?
  = link_to "New Post", new_post_path
